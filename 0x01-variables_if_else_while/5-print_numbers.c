#include <stdio.h>
/**
 * main - to print all single digit numbers of base 10.
 * discription- code should be in the main function.
 * Return: Always 0 (success)
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	putchar('\n');
	return (0);
}
